{
  "mode": "standalone",
  "maxRetries": 3,
  "baseDelayMs": 1000,
  "maxDelayMs": 30000,
  "circuitThreshold": 5,
  "circuitCooldownMs": 120000,
  "targets": [],

  "_x402_notes": "x402 is configured via environment variables. See .env.example for details.",
  "_pricing_notes": "Outcome-based: agents only pay when errors are successfully healed. Successes are free."
}
